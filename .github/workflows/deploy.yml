name: Deploy Streamlit App to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Install sshpass
      run: sudo apt-get install -y sshpass

    - name: Deploy to VPS
      env:
        HOST: ${{ secrets.VPS_HOST }}
        USERNAME: ${{ secrets.VPS_USERNAME }}
        PASSWORD: ${{ secrets.VPS_PASSWORD }}
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      run: |
        sshpass -p "$PASSWORD" ssh -o StrictHostKeyChecking=no $USERNAME@$HOST << EOF
          cd ~/qr-transcriber || exit 1
          git pull origin main

          docker stop qr-transcriber || true
          docker rm qr-transcriber || true

          docker build -t qr-transcriber .

          docker run -d \
            --name qr-transcriber \
            -p 8501:8501 \
            -e OPENAI_API_KEY="$OPENAI_API_KEY" \
            qr-transcriber
        EOF

